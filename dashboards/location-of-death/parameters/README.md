# Location of Death — Parameters and User Controls

This folder will document the parameters, filters and actions that genuinely drive the Location of Death dashboard.

For each control record:

```text
Name
Type: parameter / filter / dashboard action
Configuration or allowed values
Affected worksheets
Supporting calculation (if any)
Reporting purpose
```

If the dashboard uses few or no meaningful parameters, state that accurately rather than adding artificial technical evidence.